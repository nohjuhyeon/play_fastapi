from typing import Optional
from beanie import Document # 문서 형식으로 저장하기 위해 만듦

# 상속을 위한 하나의 클래스를 만듦. collection 지정을 위해

class bask(Document) : # collection에 들어갈 항목 저장
    name : Optional[str] =None
    name : Optional[str] =None
    name : Optional[str] =None